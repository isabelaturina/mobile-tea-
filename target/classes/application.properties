# Server Port
server.port=8080

# Application Name
spring.application.name=tea-api

# Firebase
firebase.database.url=https://tea-plus.firebaseio.com

# ? CONFIGURA��O DE EMAIL (MODO TESTE)
# Configura��es b�sicas - N�O PRECISA DE CREDENCIAIS REAIS
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=teste@exemplo.com
spring.mail.password=senhaqualquer

# Propriedades SMTP (n�o importam no modo teste)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# URL da aplica��o para links de redefini��o
app.base-url=http://localhost:8080

# Logging
logging.level.com.tea.tea.api=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.mail=DEBUG

# Habilitar agendamento para limpeza de tokens
spring.task.scheduling.enabled=true

# ?? CONFIGURA��O THYMELEAF (para evitar warning)
spring.thymeleaf.check-template-location=false